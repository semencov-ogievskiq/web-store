const setLoad = {type:"SET_LOAD"}

const removeLoad = {type:"REMOVE_LOAD"}

const signIn = (login,password)=>{
    return (dispatch)=>{
        dispatch(setLoad);
        return ?????
    }
}

export {signIn}